server.port=8083

## connect to database
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url = jdbc:postgresql://localhost:5432/organizationServiceDb

# input your username and password for the database
spring.datasource.username = postgres
spring.datasource.password = leibo164961

#JPA config
#config JPA to auto create in the database
Spring.jpa.properties..hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
Spring.jpa.hibernate.ddl-auto=update
Spring.jpa.show-sql = true
Spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.open-in-view=true



#spring.application.name=ORGANIZATION-SERVICE
eureka.instance.client.serverUrl.defaultZone = http://localhost:8761/eureka/
